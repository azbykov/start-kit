# Database Connection
# For local development: postgresql://user:password@localhost:5432/dbname
# For Supabase: Get from Settings → Database → Connection string (URI)
# For Neon: Get from project dashboard
DATABASE_URL="postgresql://user:password@localhost:5432/startkit_dev"

# NextAuth Configuration
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# NextAuth URL
# For local: http://localhost:3001 (or your dev port)
# For production: https://your-app.vercel.app (update after first deploy)
NEXTAUTH_URL="http://localhost:3001"

# Yandex OAuth (Optional)
# Get from https://oauth.yandex.com/client/new
# Set callback URL to: {NEXTAUTH_URL}/api/auth/callback/yandex
YANDEX_CLIENT_ID=""
YANDEX_CLIENT_SECRET=""

# API Base URL (Optional)
# Leave empty for relative paths (/api)
# NEXT_PUBLIC_API_URL=""

# Email Provider (Optional)
# Required only if using email authentication
# EMAIL_SERVER=""
# EMAIL_FROM=""

# Node Environment (automatically set by Vercel)
# NODE_ENV="production"

# Vercel Blob (for tournament documents uploads)
# Create in Vercel Project → Storage → Blob → Tokens
# Used server-side in Route Handlers
BLOB_READ_WRITE_TOKEN=""
