# Руководство по деплою приложения

Этот документ описывает варианты деплоя Next.js приложения с Prisma и PostgreSQL.

## Рекомендуемые варианты

### 1. Vercel + Supabase/Neon (Рекомендуется) ⭐

**Плюсы:**
- Лучшая интеграция с Next.js (создано той же командой)
- Автоматический деплой из Git
- Edge Functions и Serverless
- Бесплатный тариф для старта
- Отличная производительность

**Минусы:**
- Нужна отдельная БД (но это не проблема)

**Шаги:**

1. **Создайте PostgreSQL базу данных:**
   - **Supabase** (рекомендуется): https://supabase.com
     - Создайте проект
     - Скопируйте `DATABASE_URL` из Settings → Database
   - **Neon** (альтернатива): https://neon.tech
     - Создайте проект
     - Скопируйте connection string

2. **Деплой на Vercel:**
   ```bash
   # Установите Vercel CLI
   npm i -g vercel
   
   # Войдите в аккаунт
   vercel login
   
   # Деплой
   vercel
   ```

3. **Настройте переменные окружения в Vercel:**
   - Перейдите в Settings → Environment Variables
   - Добавьте:
     - `DATABASE_URL` - строка подключения к PostgreSQL
     - `NEXTAUTH_SECRET` - случайная строка (можно сгенерировать: `openssl rand -base64 32`)
     - `NEXTAUTH_URL` - URL вашего приложения (например, `https://your-app.vercel.app`)
     - Другие переменные из `.env.example`

4. **Настройка Prisma для продакшена:**
   
   ✅ **Уже настроено!** В `package.json` уже есть:
   - `postinstall`: автоматически генерирует Prisma Client при установке зависимостей
   - `build`: генерирует Prisma Client и собирает Next.js приложение
   
   Vercel автоматически выполнит эти команды при деплое.

5. **Примените схему Prisma к продакшн БД:**
   
   **Вариант 1: Используя скрипт (рекомендуется)**
   ```bash
   # Скачайте переменные окружения из Vercel
   vercel env pull .env.production
   
   # Примените схему
   DATABASE_URL="your-production-db-url" ./scripts/deploy-prisma.sh
   ```
   
   **Вариант 2: Вручную**
   ```bash
   # Локально с удаленной БД
   DATABASE_URL="your-production-db-url" npx prisma db push
   
   # Или через Vercel CLI
   vercel env pull .env.production
   npx prisma db push
   ```
   
   ⚠️ **Важно:** Примените схему **после первого деплоя**, когда у вас уже есть `DATABASE_URL` из Supabase/Neon.

**Стоимость:**
- Vercel: Бесплатно до 100GB bandwidth/месяц
- Supabase: Бесплатно до 500MB БД, 2GB bandwidth
- Neon: Бесплатно до 3GB БД

---

### 2. Railway (Всё в одном) 🚂

**Плюсы:**
- Всё в одном месте (приложение + БД)
- Простая настройка
- Автоматический деплой из Git
- Хорошая документация

**Минусы:**
- Меньше оптимизаций для Next.js, чем Vercel
- Может быть дороже на больших нагрузках

**Шаги:**

1. **Создайте аккаунт на Railway:** https://railway.app

2. **Создайте новый проект:**
   - New Project → Deploy from GitHub repo
   - Выберите ваш репозиторий

3. **Добавьте PostgreSQL:**
   - New → Database → PostgreSQL
   - Railway автоматически создаст `DATABASE_URL`

4. **Настройте переменные окружения:**
   - Variables → Add
   - Добавьте `NEXTAUTH_SECRET`, `NEXTAUTH_URL` и другие

5. **Настройте build:**
   - Settings → Build Command: `npm run build`
   - Start Command: `npm start`

6. **Примените миграции:**
   ```bash
   # Через Railway CLI
   railway link
   railway run npx prisma db push
   ```

**Стоимость:**
- $5/месяц за проект + $0.10/GB storage для БД
- Бесплатный тариф: $5 кредитов/месяц

---

### 3. Render (Всё в одном) 🎨

**Плюсы:**
- Всё в одном месте
- Бесплатный тариф для старта
- Автоматический деплой из Git

**Минусы:**
- Медленнее, чем Vercel
- Бесплатные инстансы "засыпают" после неактивности

**Шаги:**

1. **Создайте аккаунт на Render:** https://render.com

2. **Создайте PostgreSQL:**
   - New → PostgreSQL
   - Скопируйте Internal Database URL

3. **Создайте Web Service:**
   - New → Web Service
   - Подключите GitHub репозиторий
   - Настройки:
     - Build Command: `npm install && npx prisma generate && npm run build`
     - Start Command: `npm start`
     - Environment: Node

4. **Настройте переменные окружения:**
   - Environment → Add Environment Variable
   - Добавьте все необходимые переменные

5. **Примените миграции:**
   ```bash
   # Через Render Shell
   render shell
   npx prisma db push
   ```

**Стоимость:**
- Бесплатно для старта (с ограничениями)
- PostgreSQL: $7/месяц для production

---

### 4. Fly.io (Гибкий вариант) ✈️

**Плюсы:**
- Глобальное распределение
- Хорошая производительность
- Гибкая настройка

**Минусы:**
- Требует больше настройки
- Нужен Dockerfile

**Шаги:**

1. **Установите Fly CLI:**
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **Создайте приложение:**
   ```bash
   fly launch
   ```

3. **Создайте PostgreSQL:**
   ```bash
   fly postgres create
   fly postgres attach <app-name>
   ```

4. **Настройте переменные:**
   ```bash
   fly secrets set NEXTAUTH_SECRET=your-secret
   fly secrets set NEXTAUTH_URL=https://your-app.fly.dev
   ```

**Стоимость:**
- Бесплатно: 3 shared-cpu-1x VMs
- PostgreSQL: от $1.94/месяц

---

## Общие шаги для всех платформ

### 1. Подготовка переменных окружения

Создайте файл с переменными для продакшена:

```bash
# .env.production
DATABASE_URL="postgresql://user:password@host:5432/dbname"
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="https://your-app-domain.com"
NODE_ENV="production"
```

### 2. Настройка Prisma для продакшена

Убедитесь, что Prisma Client генерируется при сборке:

**package.json:**
```json
{
  "scripts": {
    "postinstall": "prisma generate",
    "build": "prisma generate && next build"
  }
}
```

### 3. Применение миграций

**Вариант 1: Через CLI (рекомендуется для начала)**
```bash
# Установите переменные окружения
export DATABASE_URL="your-production-db-url"

# Примените схему
npx prisma db push

# Или создайте миграцию (когда перейдете на миграции)
npx prisma migrate deploy
```

**Вариант 2: Через CI/CD**
Добавьте в GitHub Actions или другой CI:
```yaml
- name: Run Prisma migrations
  run: npx prisma migrate deploy
  env:
    DATABASE_URL: ${{ secrets.DATABASE_URL }}
```

### 4. Seed данных (опционально)

```bash
DATABASE_URL="your-production-db-url" npm run db:seed
```

⚠️ **Внимание:** Не запускайте seed на продакшене без необходимости!

### 5. Проверка подключения

После деплоя проверьте:
- Приложение запускается без ошибок
- База данных доступна
- API endpoints работают
- Аутентификация работает

---

## Рекомендации по выбору

### Для MVP / Стартапа:
- **Vercel + Supabase** - лучший баланс цены и производительности

### Для простоты (всё в одном):
- **Railway** - если хотите всё на одной платформе

### Для бесплатного старта:
- **Render** - бесплатный тариф для начала

### Для глобального масштабирования:
- **Fly.io** - если нужна глобальная инфраструктура

---

## Миграция с `db push` на `migrate`

Сейчас проект использует `prisma db push` для разработки. Для продакшена рекомендуется перейти на миграции:

1. **Создайте первую миграцию:**
   ```bash
   npx prisma migrate dev --name init
   ```

2. **Обновите скрипты в package.json:**
   ```json
   {
     "scripts": {
       "db:migrate": "prisma migrate deploy",
       "db:push": "prisma db push" // только для dev
     }
   }
   ```

3. **Используйте `migrate deploy` на продакшене:**
   ```bash
   npx prisma migrate deploy
   ```

---

## Troubleshooting

### Ошибка: "Prisma Client not generated"
- Убедитесь, что `prisma generate` запускается при сборке
- Проверьте, что `@prisma/client` в dependencies

### Ошибка подключения к БД
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что БД доступна из интернета (не localhost)
- Проверьте firewall настройки БД

### Ошибка миграций
- Убедитесь, что используете правильную команду (`db push` для dev, `migrate deploy` для prod)
- Проверьте, что схема синхронизирована

---

## Полезные ссылки

- [Vercel Deployment Guide](https://vercel.com/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [Railway Documentation](https://docs.railway.app)
- [Prisma Deployment Guide](https://www.prisma.io/docs/guides/deployment)
- [Next.js Deployment](https://nextjs.org/docs/deployment)

