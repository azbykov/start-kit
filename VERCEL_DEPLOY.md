# 🚀 Быстрый деплой на Vercel

Пошаговая инструкция по деплою проекта на Vercel.

## Предварительные требования

- Аккаунт на [Vercel](https://vercel.com) (бесплатный)
- Аккаунт на [Supabase](https://supabase.com) или [Neon](https://neon.tech) для PostgreSQL
- Git репозиторий (GitHub, GitLab или Bitbucket)

## Шаг 1: Создание базы данных

### Вариант A: Supabase (рекомендуется)

1. Зарегистрируйтесь на https://supabase.com
2. Создайте новый проект
3. Перейдите в **Settings → Database**
4. Скопируйте **Connection string** (URI) — это ваш `DATABASE_URL`
   - Формат: `postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres`

### Вариант B: Neon

1. Зарегистрируйтесь на https://neon.tech
2. Создайте новый проект
3. Скопируйте connection string из дашборда

## Шаг 2: Деплой на Vercel

### Через веб-интерфейс (рекомендуется)

1. Зайдите на https://vercel.com
2. Нажмите **"Add New Project"**
3. Подключите ваш Git репозиторий
4. Vercel автоматически определит Next.js
5. Нажмите **"Deploy"** (пока без переменных окружения)

### Через CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel
```

## Шаг 3: Настройка переменных окружения

После первого деплоя получите URL вашего приложения (например, `https://your-app.vercel.app`).

1. В Vercel перейдите в **Settings → Environment Variables**
2. Добавьте следующие переменные:

### Обязательные переменные:

```bash
DATABASE_URL=postgresql://...  # Из Supabase/Neon
NEXTAUTH_SECRET=...            # Сгенерируйте: openssl rand -base64 32
NEXTAUTH_URL=https://your-app.vercel.app  # Ваш URL после деплоя
```

### Опциональные переменные (если используете):

```bash
YANDEX_CLIENT_ID=...
YANDEX_CLIENT_SECRET=...
```

**Важно:** 
- Выберите окружения: **Production**, **Preview**, **Development**
- После добавления переменных нужно **передеплоить** проект

## Шаг 4: Применение Prisma схемы

После настройки переменных окружения примените схему базы данных:

### Вариант 1: Через скрипт (рекомендуется)

```bash
# Скачайте переменные окружения
vercel env pull .env.production

# Примените схему
DATABASE_URL="$(grep DATABASE_URL .env.production | cut -d '=' -f2-)" ./scripts/deploy-prisma.sh
```

### Вариант 2: Вручную

```bash
# Скачайте переменные окружения
vercel env pull .env.production

# Примените схему
npx prisma db push
```

## Шаг 5: Обновление callback URL для OAuth

Если используете Yandex OAuth:

1. Получите URL вашего приложения из Vercel
2. В настройках Yandex OAuth обновите callback URL:
   - Старый: `http://localhost:3001/api/auth/callback/yandex`
   - Новый: `https://your-app.vercel.app/api/auth/callback/yandex`

## Шаг 6: Проверка

После деплоя проверьте:

- ✅ Приложение открывается по URL
- ✅ Страница входа работает
- ✅ API endpoints отвечают (проверьте `/api/example`)
- ✅ Подключение к БД работает (попробуйте создать/просмотреть данные)

## Что уже настроено в проекте

✅ **package.json**:
- `postinstall`: автоматически генерирует Prisma Client
- `build`: генерирует Prisma Client и собирает Next.js

✅ **vercel.json**:
- Настроен build command
- Настроены таймауты для API функций (10 секунд)

✅ **Скрипты**:
- `scripts/deploy-prisma.sh`: для применения Prisma схемы

## Troubleshooting

### Ошибка: "Prisma Client not generated"
- Убедитесь, что `postinstall` скрипт выполняется
- Проверьте логи сборки в Vercel

### Ошибка подключения к БД
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что БД доступна из интернета
- Проверьте firewall настройки БД (Supabase/Neon обычно открыты)

### Ошибка: "NEXTAUTH_SECRET is missing"
- Убедитесь, что добавили `NEXTAUTH_SECRET` в переменные окружения
- Передеплойте проект после добавления переменных

## Полезные команды

```bash
# Просмотр переменных окружения
vercel env ls

# Скачать переменные окружения локально
vercel env pull .env.production

# Локальный деплой (preview)
vercel

# Продакшн деплой
vercel --prod

# Просмотр логов
vercel logs
```

## Стоимость

- **Vercel Hobby**: Бесплатно (до 100GB bandwidth/месяц)
- **Vercel Pro**: $20/месяц (60 сек таймаут, больше bandwidth)
- **Supabase Free**: Бесплатно (до 500MB БД, 2GB bandwidth)
- **Neon Free**: Бесплатно (до 3GB БД)

## Следующие шаги

После успешного деплоя:

1. Настройте кастомный домен (опционально)
2. Включите Vercel Analytics для мониторинга
3. Настройте автоматический деплой из Git (уже работает по умолчанию)

---

**Готово!** 🎉 Ваше приложение должно быть доступно по URL из Vercel.

